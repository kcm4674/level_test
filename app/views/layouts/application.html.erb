<!DOCTYPE html>
<html>
  <head>
    <title>Level</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if user_signed_in? %>
      <span>유저 이메일 : <%= current_user.email %> <%= link_to "로그아웃", destroy_user_session_path, method: "delete" %></span>
    <% else %>
      <span>로그인 해주세요!</span> <%= link_to "로그인", new_user_session_path %>
    <% end %>
    <hr/>
    
    <%= yield %>
  </body>
</html>
